# Nvim config
Currently, using:
- NVIM v0.11.6
- Build type: Release
- LuaJIT 2.1.1741730670

I used an AppImage to get the aforementioned Neovim.

Note: if you use qterminal, then you need to change the behaviour "default" to "linux". This way you can use the shortkey ctrl + h 
for switching to a window to the left.